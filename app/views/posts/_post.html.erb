<div class="posts-wrapper">
  <div class="post">
    <div class="post-head">
      <div class="thumb-img"></div>
      <div class="user-name">
  <%= link_to post.user.user_name, profile_path(post.user.user_name) %>
      </div>


    
    <div class="image center-block">
      <%= link_to (image_tag post.image.url(:medium), class:'img-responsive'), post_path(post) %>
    </div>
    <div class="post-bottom">
     <div id="drake"> 
      <div class="caption">
        <div class="user-names">
          <%= post.user.user_name %>....
          <%= post.caption %>
        </div>
      </div> 
        </div>
      <% if post.comments %>
        <% post.comments.each do |comment| %>
          <div class="comment">
            <div class="user-comments">
              <%= comment.user.user_name %>....     
              <%= comment.content %>
              <% if comment.user == current_user %>
              <%= link_to post_comment_path(post, comment), method: :delete, data: { confirm: "Are you sure?" } do %>
                <span id="jets" class="glyphicon glyphicon-remove delete-comment"></span>
              <% end %>
            <% end %>
            </div>
            <div class="comment-content">
      <% end %>
      <% end %>
    </div>
    <div class="comment-like-form row">
      <div class="like-button col-sm-1">
        <span id="rangers" class="glyphicon glyphicon-heart-empty"></span>
      </div>
      <div class="comment-form col-sm-11">
        <%= form_for [post, post.comments.build] do |f| %>
          <%= f.text_field :content, placeholder: 'Add a comment...', class: "comment_content", id: "comment_content_#{post.id}" %>
        <% end %>
      </div>
    </div>
  </div>
</div>